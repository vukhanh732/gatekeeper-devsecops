name: DevSecOps Gatekeeper Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  security-operations:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install bandit safety

      # --- SAST (Bandit) ---
      - name: SAST Scan (Bandit)
        # We allow this to "fail" silently so our custom script handles the logic
        continue-on-error: true 
        run: bandit -r . -lll -ii -f json -o bandit-report.json

      # --- SCA (Safety) ---
      - name: SCA Scan (Safety)
        continue-on-error: true
        # Safety might return non-zero exit codes, we capture the JSON
        run: safety check -r requirements.txt --json > safety-report.json || true

      # --- SECURITY GATE (The "Brain") ---
      - name: Enforce Security Policy
        run: python security_gate.py --bandit bandit-report.json --safety safety-report.json

      # --- DAST (ZAP) ---
      # We run this AFTER the gate (or before, depending on policy). 
      # Usually DAST is slow, so we gate early if SAST fails.
      - name: Build & Run App for DAST
        run: |
          docker build -t gatekeeper-test .
          docker run -d -p 5000:5000 --name test-container gatekeeper-test
          sleep 10

      - name: DAST Scan (OWASP ZAP)
        uses: zaproxy/action-baseline@v0.9.0
        with:
          target: 'http://localhost:5000'
          rules_file_name: '.zap/rules.tsv'
          cmd_options: '-a'

      # --- REPORTING ---
      - name: Upload All Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-evidence
          path: |
            bandit-report.json
            safety-report.json
            zap_report.html
